//just like a java model class, react interfaces typically MODEL some data/data type we intend to use
export interface UserInterface {
    userId?:number,
    username:string,
    password?:string,
    name:string,
}